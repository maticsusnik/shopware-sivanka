<?xml version="1.0" encoding="UTF-8"?>
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/shopware/platform/trunk/src/Core/System/SystemConfig/Schema/config.xsd">
    <card>
        <title>Optiweb Product Enquiry</title>

        <input-field type="text">
            <name>enquiryForm</name>
            <label>CMS Page ID (Enquiry Form)</label>
            <helpText>Paste the ID of the CMS page that contains the product enquiry form element. Find it in Shopping Experiences → URL of the page.</helpText>
        </input-field>

        <input-field type="bool">
            <name>wishlistEnquiry</name>
            <label>Enable bulk enquiry from the wishlist</label>
            <label lang="de-DE">Sammelanfrage vom Merkzettel aktivieren</label>
            <helpText>Adds a selection checkbox to every wishlist card and a "send enquiry" bar above the wishlist, so one enquiry can cover several products. Requires the CMS Page ID above.</helpText>
            <defaultValue>true</defaultValue>
        </input-field>

        <input-field type="text">
            <name>adminEmail</name>
            <label>Admin Notification Email</label>
            <helpText>Email address for admin notifications. Overrides the store default. Leave blank to use the store default email.</helpText>
        </input-field>
    </card>
</config>
