{% sw_extends '@Storefront/storefront/component/checkout/add-product-by-number.html.twig' %}

{# The design has no "add product by number" form. The theme already drops it from the
   filled cart (page_checkout_cart_add_product_and_shipping); since 6.7.x core also
   includes this file on the empty cart, which is the only other place it is used. #}
{% block page_checkout_cart_add_product %}{% endblock %}
